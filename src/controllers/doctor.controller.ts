import { NextFunction, Response, Request } from "express";
import { validationResult } from "express-validator";

export const doctorSignup = (
  req: Request,
  res: Response,
  next: NextFunction
) => {};
export const doctorLogin = (
  req: Request,
  res: Response,
  next: NextFunction
) => {};
export const updateDoctorById = (
  req: Request,
  res: Response,
  next: NextFunction
) => {};
export const deleteDoctorById = (
  req: Request,
  res: Response,
  next: NextFunction
) => {};
